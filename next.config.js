/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'utfs.io', // uploadthing domain added for images
        ],
    },
};

module.exports = nextConfig;
